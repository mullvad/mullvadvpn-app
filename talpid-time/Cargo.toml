[package]
name = "talpid-time"
version = "0.0.0"
authors = ["Mullvad VPN"]
description = "Time functions"
license = "GPL-3.0"
edition = "2021"
publish = false

[dependencies]
tokio = { version = "1.8", features = ["time"] }
libc = "0.2"
