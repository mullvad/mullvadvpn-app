import styled from 'styled-components';

import { Spacings } from '../../../foundations';
import { Flex } from '../../flex';

export const ProgressContentGroup = styled(Flex).attrs({
  $alignItems: 'center',
  $gap: Spacings.spacing5,
})``;
