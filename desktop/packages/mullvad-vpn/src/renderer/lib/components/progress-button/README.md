# Progress button

Button with support for several stauses, which is controlled via the `status` prop.

## Example

```tsx
<ProgressButton status="idle">
  <ProgressButton.StatusIndicator />
  <ProgressButton.Text>Do something</ProgressButton.Text>
</ProgressButton>
```
