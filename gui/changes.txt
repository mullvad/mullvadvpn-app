Revamped Mullvad device management. All app logins now have a name, and when you have too many devices you can log out other devices directly in the app
Added ability to block gambling and adult content websites
Connects to WireGuard servers faster, thanks to more efficiently verifying that the tunnel works
Uses Shadowsocks obfuscation to reach the Mullvad API. Circumvents censorship
[Windows] Fixes possible DNS leaks that could happen if split tunneling was used
[macOS] Does not consume 100% CPU if there is an issue with controlling the system's DNS settings
The escape key now navigates back one step. Shift+escape navigates back to the main view
